- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.config/git/config: git/config
    ~/.config/git/ignore: git/ignore

    ~/.npmrc:

    ~/.zsh:
    ~/.zshrc:
    ~/.zshenv:
