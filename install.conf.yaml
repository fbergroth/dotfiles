- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.config/git/config: git/config
    ~/.config/git/ignore: git/ignore

    ~/.p10k.zsh:
    ~/.zsh:
    ~/.zshrc:
    ~/.zshenv:
